# Media content (large files, user-specific).
# Everything is ignored except the bundled demo image — new files dropped in
# _Favorites (or anywhere else under Media/) stay untracked.
Media/**
!Media/_Favorites/
!Media/_Favorites/Anima_Demo.png

# Config with API keys
config.json
claude-conversations.json

# OS files
desktop.ini
Thumbs.db
.DS_Store

# Node
node_modules/
certs/cert.pem
certs/key.pem
characters.json
chat-conversations copy.json
chat-conversations.backup.json
chat-conversations.json
costs.json

# Generated character portraits and scene renders
character_images/
voices/

# Local setup/maintenance scripts and run logs (machine-specific)
scripts/
_fw_scratch/
app-workflows.json
app-replacements.json
app-prompt-index.json
debug-results.json
.claude/
Workflows/
